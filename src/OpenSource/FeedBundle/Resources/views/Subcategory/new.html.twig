{% extends 'OpenSource\FeedBundle::base.html.twig' %}

{% block content -%}
    <h1>{{title}}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('subcategory', {'category' : entity.category.name}) }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
